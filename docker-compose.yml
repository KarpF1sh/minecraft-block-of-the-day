services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2556:3000"
    volumes:
      - ./src:/usr/src/app/src
    environment:
      STATIC_DI: ${NODE_ENV}
      NODE_ENV: production
    restart: always

networks:
  default:
    driver: bridge